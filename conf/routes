# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


## TODO: Implement these
# Provide all the addresses of PatiendId
GET      /address/:patientId               controllers.AddressController.getPatientAddress(patientId: Long)

# Provide the address to delete
DELETE   /address/:addressId    controllers.AddressController.deletePatientAddress(addressId: Long)

POST     /address               controllers.AddressController.addPatientAddress()
